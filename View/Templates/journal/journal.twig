{% extends 'index.twig' %}

{% block content %}
    
<div class="col-sm-offset-2 col-sm-8">
    <div class="col-md-12">
        <h3>{{ journal.title }}</h3>
        <p>{{ journal.content | nl2br }}</p>
    </div>
    <a href="{{ offset }}/journal/edit/{{ journal.id }}">Edit this journal</a><br>
    <a href="{{ offset }}/journal">Go back to the journal list</a><br>
</div>

{% endblock %}
